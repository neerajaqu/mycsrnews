{
	"mysql": {
    "server_root_password": "<%= @settings[:default_mysql_root_password] %>",
    "bind_address": "<%= @settings[:mysql_bind_address] %>"
	},
	"redis": {
    "bind_address": "<%= @settings[:redis_bind_address] %>"
	},
  "recipes": [
    <% unless @settings[:using_aws] %>
      "iptables",
    <% end %>
    "imagemagick",
    "nginx",
    "gems",
    "mysql::server",
    "libxml",
    "newscloud_deps",
    "newscloud_database",
    "redis"
  ],
  "gems": [
  	"bundler",
  	"god"
	],
	"newscloud": {
	  "db": {
	    "user": "<%= @settings[:database][:user] %>",
      "password": "<%= @settings[:database][:password] %>",
	    "database": "<%= @settings[:database][:name] %>"
    }
  }
}
