-# @expects: local @idea_board, @title

.panel_2
  .panelBar
    %h2= t('.title')
  .list_stories
    %ul
      - idea_boards.each do |idea_board|
        %li
          = link_to idea_board.name, idea_board