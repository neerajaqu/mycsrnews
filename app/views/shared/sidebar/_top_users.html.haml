-# @expects: local 'top_users'
- cache :sidebar_top_users_html, :expires_in => 60.minutes do
  - scores ||= Score.alltime_scores 7
  - unless scores.empty?
    = render :partial => 'shared/lists/top_users_generic_list', :locals => { :list_title => t('.top_users_karma_title'), :scores => scores }