-# to do: needs way to hide if the user is already a fan

- cache :fan_application_html do
  %div{:class => "panel_2 #{defined?(widget_page) ? widget_page.css_class : ''}"}
  .panel_2
    .panelBar
      %h2= t('.become_a_fan')
    .panel_block
      .storyBlockWrap
        %p= t('.fan_details')
        %iframe{:src => "http://www.facebook.com/connect/connect.php?id=#{APP_CONFIG['application_id']}&connections=0&stream=0&locale=your_LOCALE", :frameborder => 0, :scrolling => "no", :style => "border: none; width: 300px; height: 60px;", :allowtransparency => "true"}
