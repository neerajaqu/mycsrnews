- cache :email_reminder_html, :expires_in => 1.days do
  %div{:class => "panel_1 #{defined?(widget_page) ? widget_page.css_class : ''}"}
    .panelBar
      %h2= t('email_reminder.title')
    .list_items
      %ul
        %li
          .featured
            .item_2.resource
              .itemBlock
                - if current_user.present?
                  %p= t('email_reminder.message',  :settings_link => edit_user_path(current_user, :format => 'html') )
                - else
                  %p= t('email_reminder.login_required')
                -# todo 
                  - else
                    %p= t('email_reminder.login_required', :sign_in_link => , :register_link =>  )