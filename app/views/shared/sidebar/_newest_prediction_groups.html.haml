-# @expects: local 'topic_list'

- cache :newest_prediction_groups_html, :expires_in => 180.minutes do
  - topic_list ||= PredictionGroup.active.open.newest
  = render :partial => 'shared/lists/bullet_generic_list', :locals => { :list_title => t('predictions.newest_groups'), :item_list => topic_list }