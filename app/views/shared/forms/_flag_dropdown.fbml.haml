= link_to t('.flag'), '#', :class => "flag-toggle btnComment"
.flag-box{:style => "display: none"}
  .flag-item
    - form_tag [flaggable, Flag.new], :class => "flag-form", id => 'flag-form' do 
      = select_tag :flag_type, options_for_select(Flag.flag_types.collect { |f| [f.titleize, f] })
