- content_for :sub_nav do
  = render :partial => 'shared/subnav/news_subnav'
#col_left
  .panel_1
    .panelBar
      %h2= t('.new_story_header')
    .formBox  
      - semantic_form_for @story, :url => stories_path, :html => { :multipart => true } do |form|
        = render :partial => 'story_form', :locals => { :form => form }
        - form.buttons do
          -# TODO:: switch this to locale
          = form.commit_button "Post Story", :button_html => { :class => 'btn' }

#col_right
  .panel_2
    .panelBar
      %h2
        An Easier way to Post a story
    = fb_iframe base_url(bookmarklet_panel_home_path(:canvas =>false)), :frameborder => 0, :scrolling => "no", :style => "margin: 0px; padding: 0px; width: 233px; height: 360px;"
  = render :partial => 'shared/ads_banner' ,:locals => { :slot_data => @ad_small_square }
