= gen_index_page @resources, Resource, [:title, :user_id, :votes_tally, :comments_count, :created_at, :resource_section_id], :associations => { :belongs_to => { :resource_section => :resource_section_id, :user => :user_id } }, :paginate => true
