= gen_show_page @idea_board, [:name, :description, :section]

%h2 Ideas for this Topic
= gen_table @idea_board.ideas, Idea, [:title, :user_id, :votes_tally, :comments_count, :created_at], :associations => { :belongs_to => {:user => :user_id} }
