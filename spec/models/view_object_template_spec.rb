require 'spec_helper'

describe ViewObjectTemplate do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ViewObjectTemplate.create!(@valid_attributes)
  end
end
