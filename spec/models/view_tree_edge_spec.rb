require 'spec_helper'

describe ViewTreeEdge do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ViewTreeEdge.create!(@valid_attributes)
  end
end
